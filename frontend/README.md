
  # Annotation Transfer Tool

  This is a code bundle for Annotation Transfer Tool. The original project is available at https://www.figma.com/design/gpMAOfyZhm2fTJUn76J92e/Annotation-Transfer-Tool.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  